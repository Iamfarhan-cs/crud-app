# CRUD App

Small Go REST API scaffold for CRUD operations.

## Run

```powershell
go run ./cmd/api
```

The API listens on `http://localhost:8080`.

## Endpoints

- `GET /health`
- `GET /users`
- `POST /users`
- `GET /users/{id}`
- `PUT /users/{id}`
- `DELETE /users/{id}`
